# Benchmark addendum — barn and trees

## Required benchmark asset

Canonical working ID:

`barn_with_trees_static_scene`

The user-provided image shows a **static barn with trees**. Nothing in the image is walking or mechanical. Do not reinterpret it as a vehicle, machine, settlement, industrial structure, articulated prop, or creature.

## Source identity

The source image may have an autogenerated filename such as:

- `ChatGPT Image 2.8.2026 klo 14.41.01.png`
- `ChatGPT Image 2.8.2026 klo 14.41.01(1).png`

Filenames are weak evidence. Confirm the source visually: the subject is a barn and surrounding trees in a static outdoor scene.

## Automatic profile expectations

Expected ranked base profiles:

1. `building`
2. `static_prop`
3. `unknown`

Expected traits:

- `barn`
- `trees`
- `static_environment`
- `outdoor_scene`
- `multiple_objects`
- `natural_vegetation`

Forbidden routes:

- humanoid rigging or A-pose;
- creature or quadruped rigging;
- vehicle routing;
- articulated mechanical routing;
- animation or gait generation;
- invented mechanical components;
- treating trees as attached machinery;
- claiming unseen interior geometry as proven.

## Purpose in the benchmark matrix

This case tests a static environment/building route rather than character or vehicle processing. It should prove that the pipeline can preserve a barn and vegetation, separate scene elements sensibly, avoid unnecessary rigging, and export a usable static asset or scene collection.

## Minimum proof

### Ingest and masking

- preserve the barn silhouette, roof, walls, doors/windows that are actually visible, and visible trees;
- distinguish barn, trees, ground, sky, and unrelated background where possible;
- do not remove trees merely because they are separate components;
- do not include sky or distant background as geometry unless the selected output mode intentionally creates a full environment card or backdrop;
- record occluded and unseen surfaces rather than inventing them.

### Geometry and structure audit

Report:

- object and mesh counts;
- barn and tree component candidates;
- vertices and triangles;
- connected components;
- boundary and non-manifold counts;
- thin-feature survival for branches and roof edges;
- building opening preservation where visible;
- background contamination;
- whether the output is best represented as one static scene collection or several static objects.

### Materials and textures

Preserve observable separation between:

- barn walls and roof;
- wood, stone, metal, glass, or other visible building materials;
- trunks, branches, and foliage;
- ground and background only when intentionally included.

Reject obvious sky/background smearing onto the barn or trees.

### Export

- no armature is required;
- no animation is required;
- fresh import must preserve expected object count, materials, textures, orientation, and bounds;
- provide front, side, rear, top, and three-quarter review renders when geometry permits;
- provide a source-versus-local-versus-online comparison when an online model exists.

## Required classifications

- `SOURCE_MATCH`
- `PROFILE_DISCOVERY`
- `STATIC_SCENE_ROUTING`
- `BARN_PRESERVATION`
- `TREE_PRESERVATION`
- `BACKGROUND_CONTAMINATION`
- `TOPOLOGY_QA`
- `TEXTURE_QUALITY`
- `EXPORT_QA`
- `ONLINE_REFERENCE_COMPARISON`

Use only `PROVEN`, `REJECTED`, `NOT_PROVEN`, or `BLOCKED`, each with a precise reason code.
