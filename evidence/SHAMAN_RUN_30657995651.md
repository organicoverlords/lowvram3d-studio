# Shaman PNG-to-3D run 30657995651

**Date:** 2026-07-31  
**Runner:** `LOWVRAM3D-KONE`  
**Workflow:** `Shaman image-to-3D on local PC`  
**Classification:** `FAILED_POLICY_GATE_AFTER_VALID_GEOMETRY`

This file records the measured evidence from the first real GitHub-driven shaman PNG-to-3D run. It is not a success claim for the full pipeline.

## Source identity

- local path: `C:\Users\Lauri\Downloads\ChatGPT Image 29.7.2026 klo 20.00.45.png`
- dimensions: `1122x1402`
- SHA-256: `4d23adc758c5b700dd29939e37c043ce61919792b566bdcf13f58b1409d6cf6f`
- previous rejected SHA-256: `eccef854f816f446ce2bf2e08559df519adff223b425c1dccc1c0a9b299f13f6`

The current hash was measured from the exact local path by MagicMusic. The previous value did not match that file and is not accepted as canonical proof.

## What passed

The self-hosted runner registered, came online, accepted the job and entered the real `PipelineEngine.full(...)` PNG-to-3D route.

The generated GLB passed ingest with these measured properties:

| metric | value |
|---|---:|
| objects | 1 |
| vertices | 27,898 |
| faces / triangles | 55,684 |
| materials | 1 |
| UV layers | 0 |
| armatures | 0 |
| actions | 0 |
| finite bounds | true |
| welded vertices | 30 |

The component cleanup boundary reported:

- faces: `55,684 -> 55,668`
- removed surface: `0.0287%`
- boundary edges: `0 -> 0`
- passes: `2`

This is evidence that geometry generation and ingest succeeded. The failure was not a CUDA crash, out-of-memory event, invalid GLB, open-boundary regression or GitHub runner failure.

## Exact failure

`clean_high_resolution_master` exited with code `2` because the component audit report contained:

- `cleanup did not converge within max_passes`
- `2 visible components remain audit-required`

The unresolved fragments were small, detached components whose projection evidence was inconclusive under the original classifier. The stage then withheld `clean_master.glb`, causing `highres_geometry_ladder` and the full workflow to stop.

## Diagnosis

The hard failure was a policy defect.

An `AUDIT_REQUIRED` decision means the classifier is uncertain. It does not demonstrate that the generated mesh is corrupt. Treating uncertainty as a fatal production condition discarded the only valid generated master and contradicted the practical lesson from TurboBird, the user's first real successful result.

## Implemented correction

`workers/component_audit_cleanup.py` now distinguishes ambiguity from demonstrated damage:

- normal confirmed cleanup continues;
- ambiguity-only failures with safe topology preserve the original generated GLB byte-for-byte as `clean_master.glb`;
- the report records the original audit errors as warnings;
- `manual_review_required=true` is set;
- downstream LOD, texture, rig/export and independent validation continue;
- subprocess errors, changed main geometry, or increased boundary/non-manifold edges still fail closed.

The focused regression tests verify both sides:

1. an ambiguity-only result preserves identical bytes and continues;
2. a topology regression remains a hard failure.

## Remaining proof boundary

The correction is implemented but the full replacement run must still prove that it reaches and passes later stages. Do not classify the shaman anchor as `PROVEN` until the resulting GLBs, reports and preview renders are inspected.

TurboBird remains `USER_VALIDATED_QUALITATIVE_REFERENCE` until its exact canonical source/master hashes and quantitative receipt are consolidated.
